<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:designerViewModels="clr-namespace:FeedMonitor.ViewModels.Designer"
		d:DataContext="{d:DesignInstance designerViewModels:DesignerShellViewModel, IsDesignTimeCreatable=True}"
		d:DesignHeight="538.4"
		mc:Ignorable="d"
		x:Class="FeedMonitor.Views.ShellView"
		Background="Black"
		MaxWidth="500">
	<Border Padding="12">
		<DockPanel>
			<ItemsControl DockPanel.Dock="Top" ItemsSource="{Binding Items}">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<StackPanel Orientation="Horizontal" />
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>
				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<TextBlock FontSize="30"
								   Foreground="White"
								   Margin="6"
								   Text="{Binding DisplayName}" />
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
			<ContentControl x:Name="ActiveItem" />
		</DockPanel>
	</Border>
</Window>
