<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:designerViewModels="clr-namespace:FeedMonitor.ViewModels.Designer"
			 xmlns:models="clr-namespace:FeedMonitor.Models"
			 d:DataContext="{d:DesignInstance designerViewModels:DesignerItemsViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="300"
			 d:DesignWidth="500"
             mc:Ignorable="d"
			 x:Class="FeedMonitor.Views.ItemsView">
    <Grid>
		<ListBox Background="Transparent" ItemsSource="{Binding FeedItems}">
			<ListBox.ItemTemplate>
				<DataTemplate DataType="{x:Type models:FeedItem}">
					<Border Padding="6">
						<StackPanel>
							<TextBlock FontSize="20" Foreground="White" Text="{Binding Title}" />
							<TextBlock FontSize="10" Foreground="#888" Text="{Binding FeedTitle}" />
						</StackPanel>
					</Border>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
    </Grid>
</UserControl>
