<UserControl x:Class="FeedMonitor.Views.MessageBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:designerViewModels="clr-namespace:FeedMonitor.ViewModels.Designer"
			 xmlns:viewModels="clr-namespace:FeedMonitor.ViewModels"
             mc:Ignorable="d"
			 d:DataContext="{d:DesignInstance designerViewModels:DesignerMessageBoxViewModel, IsDesignTimeCreatable=True}">
    <Border Background="Black" Padding="12">
		<StackPanel Orientation="Vertical">
			<TextBlock FontSize="25" Foreground="White" Text="{Binding Title}" />
			<TextBlock FontSize="14" Foreground="White" Margin="12" Text="{Binding Message}" TextWrapping="Wrap" />
			<StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
				<Button>Yes</Button>
				<Button>No</Button>
			</StackPanel>
		</StackPanel>
	</Border>
</UserControl>
